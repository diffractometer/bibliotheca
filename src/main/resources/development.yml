env: development

dbConfig:
  host: db.wingdingos.com
  port: 3306
  user: postgres
  pass: 1234

serverConfig:
  port: 8080
  redirectUrl: /404.html

qrCodeConfig:
  baseUrl: bibliotheca.wingdingos.com

s3Config:
  bucketName: pureshit-production
  region: us-east-1
  folder: bookshelf_cells

prompt: |
  For each book visible in the images, provide the title and author in CSV format, be strict about CSV formatting with only the title and author in your response. If a book's title or author cannot be determined, respond with 'null' in the appropriate field. List each book from each image on a new line.